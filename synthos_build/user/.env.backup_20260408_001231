# Synthos Retail Node — Environment Configuration
# Generated by install_retail.py on 2026-04-08 00:06:42 UTC
# DO NOT SHARE — contains private credentials
# chmod 600 enforced by installer

# ── ENCRYPTION ────────────────────────────────────────────────
# Generated once at install. Never regenerate — doing so destroys all
# encrypted customer data (emails, passwords, Alpaca credentials).
ENCRYPTION_KEY=2cBEdWTddBpfAfgmm1qpT4j8bN__dHDppdiJwiJQSt8=

# ── SYSTEM IDENTITY ───────────────────────────────────────────
PI_ID=synthos-pi-retail
PI_LABEL=
PI_EMAIL=

# ── ADMIN ACCOUNT ─────────────────────────────────────────────
# Portal admin credentials — set during install.
ADMIN_NAME=Patrick McGuire
ADMIN_EMAIL=personal_prometheus@icloud.com
ADMIN_PASSWORD=AMEND-creator-sing

# ── OWNER CUSTOMER ACCOUNT ────────────────────────────────────
# Mirrors admin credentials — single-operator setup.
OWNER_NAME=Patrick McGuire
OWNER_EMAIL=personal_prometheus@icloud.com
OWNER_PASSWORD=AMEND-creator-sing
# 'standard' or 'early_adopter' — controls which pricing experience you see.
OWNER_PRICING_TIER=standard
# Populated automatically on first portal startup — do not edit manually.
OWNER_CUSTOMER_ID=

# ── TRADING SETTINGS ──────────────────────────────────────────
STARTING_CAPITAL=1000
OPERATING_MODE=SUPERVISED
AUTONOMOUS_UNLOCK_KEY=
TRADING_MODE=PAPER
CLOSE_SESSION_MODE=conservative
MAX_POSITION_PCT=0.10
MAX_SECTOR_PCT=25

# ── PORTAL ────────────────────────────────────────────────────
PORTAL_PORT=5001
PORTAL_PASSWORD=
PORTAL_SECRET_KEY=d5d82670f649681308db98055efb9296ed5aa8b2f5c2162faf576db370c2a4ee
# Set to your Cloudflare tunnel URL once active.
PORTAL_BASE_URL=
SUPPORT_EMAIL=synthos.signal@gmail.com

# ── API KEYS — restore from backup ────────────────────────────
# These are intentionally blank after a fresh install.
# Restore your backup archive to populate them, or enter manually.
ANTHROPIC_API_KEY=
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_BASE_URL=https://paper-api.alpaca.markets
LICENSE_KEY=

# ── NETWORK NODES ────────────────────────────────────────────
# Monitor node: pi2w_monitor_node (Raspberry Pi Zero 2W, LAN-only)
# DHCP-assigned IP — update to static IP when ethernet switch is installed.
# MONITOR_TOKEN must exactly match SECRET_TOKEN on pi2w_monitor_node .env
# See MEMORY.md — Future Planning Notes for IP finalization checklist.
MONITOR_URL=http://192.168.203.10:5000
MONITOR_TOKEN=synthos-default-token
# Company node: pi4b — forwards alerts and event queue
# Update IP alongside MONITOR_URL when ethernet switch is installed.
COMPANY_URL=http://192.168.206.172:5010

# ── ALERTS — restore from backup ──────────────────────────────
RESEND_API_KEY=
ALERT_FROM=
USER_EMAIL=
GMAIL_USER=
GMAIL_APP_PASSWORD=
ALERT_PHONE=
CARRIER_GATEWAY=tmomail.net

# ── OPTIONAL ──────────────────────────────────────────────────
GITHUB_TOKEN=
# ── SCOOP VOLUME CONTROLS ─────────────────────────────────────
# Master dispatch switch. Set to true when ready to send email alerts.
SCOOP_ENABLED=false
# 0=critical only  1=+high  2=+medium  3=all
SCOOP_MIN_PRIORITY=0
# ── STRONGBOX ─────────────────────────────────────────────────
STRONGBOX_STALE_HOURS=48
PI_EVENTS_RETAIN_DAYS=30
